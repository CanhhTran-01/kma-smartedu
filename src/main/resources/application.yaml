server:
  port: 8080

spring:
  application:
    name: kma-smartedu
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}  # mặc định dev, prod override bằng env var
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
